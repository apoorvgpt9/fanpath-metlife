# PROGRESS.md

Rolling build state for **PromptWars Challenge 4 — Smart Indoor Navigation**.

DECISIONS.md is the constitutional spec. This file is the running log of what actually got built and validated. When they drift, this file wins over the Notion Progress Tracker (which mirrors this file), and DECISIONS.md wins over this file on architectural claims.

**Deadline:** July 19, 2026.

**Live URL:** <https://fanpath-metlife-973486326780.asia-south1.run.app>

**Repo:** <https://github.com/apoorvgpt9/fanpath-metlife>

---

## Phase status

| Phase | Description                                          | Status              | Machine validation | Intent validation | Notes                                             |
| ----- | ---------------------------------------------------- | ------------------- | ------------------ | ----------------- | ------------------------------------------------- |
| 0     | Skeleton, config, Day-1 deploy                       | DONE                | 2026-07-13         | 2026-07-13        | Live URL green; commit c81776d; intent validation passed with 3 non-blocking notes |
| 1     | Graph (blocker) + auth + schema                      | DONE                | 2026-07-13         | 2026-07-13         | 36 nodes, 54 edges (post-patch); intent validation passed for main build + patch; 11 PASS/9 SKIP/0 FAIL |
| 2     | Pathfinding + Layer-2 tests                          | DONE              | 2026-07-13         | 2026-07-13         | 19 pathfinding tests; engine 99%, loader 100%; 12 PASS/8 SKIP/0 FAIL; CI green, /health 200 confirmed |
| 3     | Intent + Guide agents + Gemini pre-flight            | DONE                | 2026-07-14         | 2026-07-14        | 38 new tests (19 intent, 8 guide, 11 factory); coverage 98.88%; 14 PASS/6 SKIP/0 FAIL; preflight pass (flash 200, pro 200). Built on Copilot. |
| 4     | Endpoints + frontend + renderer + closures           | MACHINE-VALIDATED | 2026-07-14 (4A+4B+closeout+gap-close) | —                 | 4A+4B+closeout+gap-closing pass (Entry #28 amenity resolution, `GET /` redirect) all green. Numbers superseded by later phases building on this work — current state (189 tests, 98.95% cov, 23/0/0 verify-docs) reflects Phases 5-6's additions on top, live at revision `fanpath-metlife-00010-wcc`. Left at MACHINE-VALIDATED (not DONE) since the same four manual-browser checks Phase 6 also lists remain unconfirmed — note the inconsistency: Phase 6 is marked DONE despite the identical open item. Worth deciding: either both flip to DONE once the manual checks land, or neither should have without them. |
| 5     | Presentation pass                                    | DONE  | 2026-07-14                  | 2026-07-14                 | Docstrings on all public functions/models; Gemini JSON-parse retry; OWASP Top 10 complete in SECURITY.md; pip-audit in CI; 186 tests, 98.95% cov, 23/0/0 verify-docs; deployed revision `fanpath-metlife-00009-tnh` |
| 6     | Final gauntlet + submit                              | DONE  | 2026-07-14                  | 2026-07-14                 | Machine gauntlet green (189 tests, 98.95% cov, 23/0/0 verify-docs, pip-audit clean); live-URL gauntlet green; docs frozen; four manual-browser checks flagged for human verification; history role-mismatch bug found and fixed |

**Status values:** `NOT STARTED` / `IN PROGRESS` / `MACHINE-VALIDATED` / `INTENT-VALIDATED` / `DONE`.

---

## Revised schedule (as of July 13, 2026 — compressed from 10 days to 6 build days + submission day)

**Why this changed:** the original Phase Plan assumed a 10-day window at 2-3 hrs/day starting ~July 8. As of July 13, Phase 0 has not yet closed and only 6 build days remain before the July 19 deadline. Working capacity increased to 4-5 hrs/day to compensate, giving ~24-30 hours against ~25-26 hours of planned Phase 0-5 work — feasible, but with effectively zero slack. Two decisions were locked in to protect that margin: a hard 3-hour cap with a predetermined fallback on Phase 1a graph construction (DECISIONS.md Entry #27), and a tool-allocation change routing Phases 2 and 3 to Claude Code instead of Copilot, since a Copilot drift/redo cycle on the discriminated-union contracts in those phases is not recoverable inside this schedule.

| Day | Date | Phase | Budgeted hours | Tool |
| --- | --- | --- | --- | --- |
| 1 | Jul 13 | Phase 0 — skeleton, config, Day-1 deploy | ~3 | Copilot |
| 2 | Jul 14 | Phase 1 — graph (3hr cap, Entry #27) + auth + Firestore schema | ~4.5-5 | Copilot (1b-1d); graph draft via Gemini |
| 3 | Jul 15 | Phase 2 — pathfinding engine + Layer-2 unit tests | ~4-5 | **Claude Code** |
| 4 | Jul 16 | Phase 3 — Intent + Guide agents + Gemini pre-flight + contract tests | ~4-5 | **Claude Code** |
| 5 | Jul 17 | Phase 4 — endpoints + frontend + renderer + closures + integration + full deploy | ~4-5 | Copilot |
| 6 | Jul 18 | Phase 5 (compressed, ~2hr) + contingency buffer for spillover | ~4-5 | — |
| 7 | Jul 19 | Phase 6 — final gauntlet + submit. No new features. | ~1-2 | — |

**Correction (2026-07-14):** The Claude Code routing for Phases 2-3 planned in the table above was reversed same-day (2026-07-13) after Phase 2 shipped clean on Copilot. The project stays on Copilot for the entire codebase, including Phase 3. Table left as a record of what was planned; see the Running Log entries for Phases 2 and 3 for what actually shipped and which tool built each.

**Contingency logic:** Day 6's buffer is sized to absorb spillover from the two most likely sources — Phase 1a (graph, despite the cap) and Phase 3 (agent behavior, given the just-discovered Gemini model migration in Entry #26 is untested against real prompts yet). Phase 5 compresses cleanly because Evaluator Insights already established presentation as a cleared floor, not a differentiator, for this rubric.

**Open item:** confirm the exact submission portal close time on July 19 — determines whether Day 7 has real working hours or the effective deadline is end-of-Day-6.

---

## `make verify-docs` — verifiable claim set

Per DECISIONS.md Entry #24, Layer 2 requires an explicit set of claims the CI script checks. This is that set. Add to it as new claims appear; do not delete claims once added (mark them "retired" instead, with a rationale).

Each row is: claim from DECISIONS.md → the grep or file check that verifies it.

| # | Claim                                                                               | Verification                                                                                              |
| - | ----------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- |
| 1 | Coverage floor is 95% (Entry #13, #21)                                              | `pyproject.toml` contains `--cov-fail-under=95` OR Makefile `test` target has `--cov-fail-under=95`       |
| 2 | ruff has C901, PLR0912, PLR0915 in `select` (Entry #13)                             | `pyproject.toml` `[tool.ruff]` `select` list contains `"C901"`, `"PLR0912"`, `"PLR0915"`                  |
| 3 | ruff `max-complexity = 10` (Entry #13)                                              | `pyproject.toml` `[tool.ruff.lint.mccabe]` `max-complexity = 10`                                          |
| 4 | Function-length threshold is 80 lines (Phase 0 detail)                              | `scripts/check_function_length.py` contains `MAX_FUNCTION_LINES = 80` (or equivalent constant)            |
| 5 | Fan profile has three fields (Entry #25 supersedes #7)                              | Firestore write path in code writes exactly `seat_section`, `accessibility_flags`, `preferred_language`   |
| 6 | Amenity enum has six values (Entry #11)                                             | Enum definition contains exactly `restroom`, `food`, `merchandise`, `atm`, `first_aid`, `charging_station`|
| 7 | Language enum has five values (Entry #25)                                           | Enum definition contains exactly `en`, `es`, `fr`, `pt`, `ar`                                             |
| 8 | Accessibility flag enum has four values (Entry #7)                                  | Enum definition contains exactly `wheelchair`, `no_stairs`, `stroller`, `visual_impairment`               |
| 9 | Edge accessibility enum has four values (Entry #8)                                  | Enum definition contains exactly `stairs_only`, `ramp`, `elevator`, `level`                               |
| 10| Pathfinding output is a discriminated union (Entry #17)                             | Grep for `RouteFound`, `RouteBlocked`, `RouteImpossible` in the pathfinding module                        |
| 11| Intent Agent output is a discriminated union (Entry #9)                             | Grep for `ResolvedRequest`, `AmbiguousRequest`, `UnresolvableRequest` in the intent module                |
| 12| Six endpoints exist (Entry #19)                                                     | FastAPI app registers exactly `POST /profile`, `GET /profile`, `POST /navigate`, `POST /staff/closures`, `GET /staff/closures`, `GET /health` |
| 13| Fan endpoints use Firebase Anonymous Auth (Entry #6, #19)                           | Auth dependency in code is applied to the three fan endpoints                                             |
| 14| Staff endpoints use STAFF_TOKEN (Entry #18)                                         | Auth dependency in code is applied to the two staff endpoints; reads `STAFF_TOKEN` env var                |
| 15| FastAPI is initialized with `redirect_slashes=False` (Entry #13 carryover)          | Grep for `redirect_slashes=False` in the app initialization file                                          |
| 16| Dockerfile uses `--no-server-header` (Entry #13 carryover)                          | Grep for `--no-server-header` in the Dockerfile                                                            |
| 17| Model tier decision is documented (Entry #13)                                       | DECISIONS.md contains an amendment recording the Phase 3 pre-flight outcome; code's factory calls match   |
| 18| Graph JSON is loaded at startup, not from Firestore (Entry #8)                      | Grep confirms `metlife_graph.json` is read via file I/O; no Firestore call fetches graph topology         |
| 19| `venue_state` is read on every navigate request (Entry #16)                         | Navigate handler contains a `venue_state` fetch; no cache decorator applied                               |
| 20| Error contract uses `K_SERVICE` for detail toggling (Entry #23)                     | Error handler code contains a check on `os.environ.get("K_SERVICE")`                                     |
| 21| Frontend is static HTML + vanilla JS (Entry #20)                                    | `static/` contains `fan.html`, `fan.js`, `staff.html`, `staff.js`, `style.css`; no `TemplateResponse`/`Jinja2Templates`/`from jinja2` anywhere under `app/`; `app/main.py` mounts `StaticFiles(directory=...)` |
| 22| SVG rendering is deterministic (Entry #12)                                          | `app/rendering/svg_renderer.py` exists, defines `render_route`, contains no reference to `gemini_factory`/`google.genai`/`GeminiClient`/`explain_route`; `app/routes.py` calls `render_route`                |
| 23| Amenity-type destination resolution (Entry #28)                                     | `app/agents/schemas.py` has `destination_amenity_type` on `ResolvedRequest` with a `@model_validator` enforcing mutual exclusion; `app/pathfinding/engine.py` defines `find_nearest_amenity`; `app/routes.py` calls it |

**Retired claims:** _(none yet)_

**Notes for the script author (Phase 0 sub-task):**

- Machine-check the file-existence and grep claims first. Enum-value claims can be checked either by grep or by importing the enum in the script and comparing sets.
- Do NOT try to check architectural rationale ("staff can only toggle closures"). That belongs to the intent-validation gate, not the CI script.
- The script should print per-claim pass/fail so a failure is diagnosable.
- Wire the script into `make verify-docs` and into CI.

---

## Running log

Append an entry at each phase close: date, what shipped, validation command outputs, deviations from plan, decisions amended in DECISIONS.md.

- **2026-07-13 — Phase 0 (MACHINE-VALIDATED, awaiting intent validation).** Shipped: `pyproject.toml` (ruff select E/F/W/C901/PLR0912/PLR0915/I/B, max-complexity=10, `--cov-fail-under=95`), Makefile (lint/test/verify-graph/verify-docs/run/deploy), `scripts/check_function_length.py` (AST-based, MAX_FUNCTION_LINES=80), `scripts/verify_graph.py` (placeholder — skips until data/metlife_graph.json exists), `scripts/verify_docs.py` (6 real PASS: claims 1-4, 15, 16 / 14 SKIP for later phases / 0 FAIL), `app/main.py` (FastAPI `redirect_slashes=False`, CORS from `ALLOWED_ORIGIN`, security-headers middleware, `GET /health`), `Dockerfile` (python:3.12-slim + `--no-server-header`), `.github/workflows/ci.yml`, `.env.example`, `.gitignore`, `tests/test_main.py` (7 tests, 100% app coverage). Pre-flight: gcloud project=`promptwars-c4-metlife`, billing enabled, Firebase Anonymous Auth enabled. Deploy: `gcloud run deploy` to `asia-south1`; required IAM roles granted to compute default SA (`roles/cloudbuild.builds.builder`, `roles/storage.objectViewer`, `roles/artifactregistry.writer`, `roles/logging.logWriter`). Live URL returns `{"status":"ok"}` with HTTP 200. Commit `c81776d` pushed to origin/main. No DECISIONS.md amendments this phase.
- **2026-07-13 — Phase 0 intent validation passed → DONE.** Three non-blocking notes: (1) `.env.example` had a real project id (`promptwars-c4-metlife`) as the placeholder for `FIREBASE_PROJECT_ID` — not a secret, but bad hygiene in an example file; fixed to `your-firebase-project-id-here` before starting Phase 1. (2) Dockerfile hardcodes port 8080 rather than reading `$PORT` per Cloud Run's contract — works today because Cloud Run defaults to 8080, deferred to Phase 5 presentation pass. (3) Local venv is Python 3.13, CI and the deployed container are Python 3.12; both satisfy `requires-python = ">=3.12"`, no risk.
- **2026-07-13 — Phase 1 (MACHINE-VALIDATED, awaiting intent validation).** Shipped: **1a** `scripts/draft_graph.py` (Gemini 3.5-flash draft generator; Entry #26 model string), `data/metlife_graph.draft.json` (36 nodes, 51 edges), `scripts/_apply_graph_corrections.py` (auditable manual-correction record: section-uniqueness fixes for premium clubs, `coaches_club` edge `stairs_only`→`elevator`, filled 100/200-east section gaps), `data/metlife_graph.json` (final: 36 nodes, 51 edges). Entry #27 3-hour cap NOT hit — draft 16:44:08Z, verify-graph green ~17:04Z (~20 min). **1b** `scripts/verify_graph.py` now enforces: schema, section uniqueness, orphans, self-loops, full-graph connectivity, accessibility-subgraph connectivity, amenity/edge-accessibility enum conformance, positive walk-time; soft warning for edges >15 min. **1c** `app/auth/firebase.py` (`verify_fan_token` dep, Entry #23-shaped 401 payload with `K_SERVICE`-gated detail, idempotent Firebase init) + 13 auth tests. **1d** `app/models/enums.py` (`AccessibilityFlag`/`PreferredLanguage`/`AmenityType`/`EdgeAccessibility`), `app/firestore/fans.py` (Entry #15 schema, enum validation on write, `FanProfile` dataclass), `app/firestore/venue_state.py` (single `current` doc, sorted-deduped writes) + 12 unit tests. `scripts/verify_docs.py` claims 5–9 now real; claims 13/14/18 SKIP messages refined. Machine validation: `make lint` clean (9 files), `make test` 36 passed, coverage 98.43% (floor 95), `make verify-graph` OK, `make verify-docs` 11 PASS/9 SKIP/0 FAIL, live URL still 200. DECISIONS.md amended: Entry #27 Outcome recorded.
- **2026-07-13 — Phase 1 patch (caught during intent validation, before Phase 2 started).** Three related fixes bundled: (1) corrected graph had zero `stairs_only` edges, making Entry #7's stairs-warning + Entry #9's accessibility Dijkstra observationally identical to unfiltered routing — added 3 parallel `stairs_only` edges (100↔200 north/west, 200↔300 north) at 2 min each vs. the accessible 4–5 min alternatives, so accessibility filtering now produces a visible difference; graph now 36 nodes / 54 edges. (2) `scripts/verify_graph.py` gained a hard check that every node has a non-empty `landmark_aliases` list of non-empty strings (Phase 3 Intent Agent depends on this; existed in the data but was never validated). (3) `python-dotenv` was a listed dependency with no import site — wired `load_dotenv()` into `app/main.py` (safe no-op when no `.env`; confirmed dotenv 1.2.2 returns `False` without raising) and added `test_module_imports_without_env_file` proving the import path is `.env`-optional. Would have surfaced only during Phase 4 integration testing per Entry #21 if not caught here. DECISIONS.md amended: Entry #27 Outcome appended (no new numbered entry — this is a data-quality correction, not an architectural change).
- **2026-07-13 — Phase 2 (MACHINE-VALIDATED, awaiting intent validation).** Shipped: `app/graph/loader.py` (Node/Edge/Graph frozen dataclasses; `load_graph(path)` for arbitrary fixture files and `load_default_graph()` for the real MetLife JSON; rejects duplicate zone_ids and missing top-level keys; NOT yet wired into `app/main.py` — Phase 4). `app/pathfinding/engine.py` (Dijkstra with node/edge/accessibility filtering; discriminated union `RouteFound | RouteBlocked | RouteImpossible` per Entry #17; `RouteFound.traverses_stairs_only` flag for Entry #7's stairs-warning safety check; `_classify_blocked` distinguishes disconnected graph from closure-blocked from accessibility-blocked and names the specific closed node/edge or closed accessible edge in the reason string, not generic "no route"; module split into `_build_filtered_adjacency`/`_dijkstra`/`_accessible_closed_edges`/`_closure_reason`/`_classify_blocked`/`find_route` per Entry #13 Code Quality lever, all under the 80-line cap with max complexity 10). `tests/fixtures/small_graph.json` (8-node synthetic graph — not the real MetLife graph per Entry #21 Layer 2; includes `island` for RouteImpossible and stairs_only shortcuts b-e and c-g for accessibility observability). `tests/unit/test_pathfinding.py` (19 tests: all five Entry #21 required cases named explicitly — `test_basic_shortest_path`, `test_accessible_only_path`, `test_path_with_closures_reroutes`, `test_route_blocked_by_closure_under_accessibility`, `test_route_impossible_when_disconnected` — plus direct assertions of the `traverses_stairs_only` flag both ways, direction-agnostic edge closures, closed-node handling, unknown zone_ids, origin==destination, and loader error paths). `scripts/verify_docs.py` claim #10 upgraded to a real PASS check (greps engine.py for the three union members); claim #18 SKIP message refined per Phase 2 spec. Machine validation: `make lint` clean (13 files), `make test` 56 passed, coverage 98.75% (app/graph/loader.py 100%, app/pathfinding/engine.py 99%, floor 95), `make verify-graph` OK (36 nodes / 54 edges), `make verify-docs` 12 PASS / 8 SKIP / 0 FAIL, live URL `/health` HTTP 200. No DECISIONS.md amendments — Phase 2 implements Entry #9/#17 as already specified.
- **2026-07-14 — Phase 3 (MACHINE-VALIDATED → INTENT-VALIDATED → DONE).** Shipped: `app/agents/gemini_factory.py` (two-tier `GeminiClient` wrapper over `google.genai.Client.models.generate_content`; `flash()`→`gemini-3.5-flash`, `pro()`→`gemini-3.1-pro-preview` per Entry #26; env-var overridable; raises `GeminiTimeoutError`/`GeminiServiceError` — HTTP mapping deferred to Phase 4 per Entry #23). `app/agents/schemas.py` (Pydantic models: `ResolvedRequest | AmbiguousRequest | UnresolvableRequest` per Entry #9; `ProfileComplete | ProfileIncomplete | ProfileFailed` per Entry #7; `ConversationTurn` per Entry #10; all with literal `type` discriminators, `extra="forbid"`, `frozen=True`). `app/agents/intent.py` (`extract_profile` + `parse_navigation_request`; Intent Agent on Pro tier per Entry #13; grounds navigation results against `graph.nodes` — rejects model-invented zone_ids with `GeminiServiceError`; `ProfileIncomplete` reserved for missing `seat_section` only per Entry #7; empty accessibility → `ProfileComplete` not follow-up). `app/agents/guide.py` (`explain_route` dispatching on `RouteFound | RouteBlocked`; Guide Agent on Flash tier per Entry #13; stairs-warning appended deterministically in code with per-language strings for all 5 languages per Entry #25 — not delegated to Gemini; `RouteBlocked` produces text-only explanation per Entry #17, no SVG call). `scripts/gemini_preflight.py` (re-runnable standalone: flash 200 2.2s, pro 200 9.4s — exit 0). `scripts/verify_docs.py` claims #11 and #17 upgraded to real PASS checks. `CLAUDE.md` added by Copilot (un-spec'd — 150-line Claude Code configuration file summarizing DECISIONS.md governance; kept as a harmless guardrail). Tests: `tests/contract/test_intent_agent.py` (19 tests: 3 navigation-parse variants, 3 profile-extraction variants, plus edge cases — unknown zone rejection, malformed JSON, unknown/missing type, validation failure, empty input short-circuit, history injection verification), `tests/contract/test_guide_agent.py` (8 tests: zone names in output, stairs-warning append/omit logic for both flag conditions, RouteBlocked prose, Spanish route + Spanish stairs warning, Arabic route — Entry #25 non-English coverage), `tests/unit/test_gemini_factory.py` (11 tests: default/override model strings, API key guard, success/error/timeout classification, MIME type passthrough, empty response). Machine validation: `make lint` clean (18 files), `make test` 94 passed, coverage 98.88% (gemini_factory 100%, intent 100%, guide 95%, schemas 100%; floor 95%), `make verify-graph` OK, `make verify-docs` 14 PASS / 6 SKIP / 0 FAIL (+2 from Phase 2's 12), `python scripts/gemini_preflight.py` exit 0. Intent validation: independently cloned and verified by the advisory session — all Entry #7/#9/#10/#13/#17/#23/#25/#26 compliance confirmed; scope boundary verified (zero diff to main.py, graph/, pathfinding/, firestore/, auth/). No DECISIONS.md amendments — Phase 3 implements Entries #7/#9/#10/#13/#25/#26 as already specified. Commit `f582692`. No deploy this phase (no endpoint changes).
- **2026-07-14 — Phase 4A (MACHINE-VALIDATED, awaiting intent validation + deploy).** Shipped the backend half of Phase 4 — the six-endpoint HTTP surface with real auth, closures, error contract, and rate limiting — leaving Phase 4B (SVG renderer, `static/fan.html`, `static/staff.html`) as the next milestone. Files: `app/errors.py` (`error_payload` centralises Entry #23's `{type, category, message, detail}` shape; `raise_error` wraps `HTTPException` with that payload; `is_error_payload` guard used by the main handler; `K_SERVICE`-gated `detail`). `app/graph/edge_id.py` (`edge_id(a, b) = "min__max"`, direction-agnostic; `parse_edge_id` inverse — bridges `venue_state.closed_edges: list[str]` to `find_route(closed_edges: set[tuple[str,str]])`; rejects empty endpoints and endpoints containing the reserved `__` separator). `app/auth/staff.py` (`verify_staff_token` FastAPI dependency; reads `STAFF_TOKEN` env; constant-time `hmac.compare_digest`; Bearer-header extraction; Entry #23 401 on miss; exports `StaffAuth` `Annotated` alias). `app/schemas.py` (Pydantic v2 request/response bodies, all `extra="forbid"`: `ProfileOnboardRequest`, `ProfileResponse` / `ProfileIncompleteResponse` / `ProfileFailedResponse`, `NavigateRequest` / `NavigateResponse`, `ClosureToggleRequest` / `ClosureStateResponse`, `ErrorResponse`). `app/rate_limit.py` (extracted from `main.py` to break the `main → routes → main` circular import; slowapi `Limiter` with SHA-256-truncated `Authorization` header as key, IP fallback; `FAN_LIMIT="60/minute"`, `STAFF_LIMIT="30/minute"`, `/health` deliberately undecorated). `app/routes.py` (all six endpoints with `@limiter.limit()` decorators; fan endpoints depend on `FanUid`, staff on `StaffAuth`; `post_navigate` calls `venue_repo.read_state(fs)` on every request per Entry #16, decodes closures via `_decode_closures`, dispatches on `ResolvedRequest | AmbiguousRequest | UnresolvableRequest`, then on `RouteFound | RouteBlocked | RouteImpossible`; `AmbiguousRequest` and `RouteBlocked` are 200 responses per Entry #17; `_map_gemini_error` maps `GeminiTimeoutError`→504 transient, `GeminiError`→502 transient; `_validate_edge_target` rejects closures against unknown zones or non-existent edges; `_handle_navigation_parse` / `_mutate_state` / `_validate_edge_target` factored out to keep every function under the 80-line cap and max-complexity 10). `app/main.py` rewritten: custom `HTTPException` handler unwraps the Entry #23 payload so error bodies are flat at the top level (fixes the Phase 3 double-wrap bug that produced `{"detail": {"type": "error", ...}}`); `RateLimitExceeded` handler emits 429 transient; graph loaded via `load_default_graph()` at startup and stashed on `app.state.graph` per Entry #8; `app.state.firestore_client_factory` injected for testability; `SecurityHeadersMiddleware` and CORS retained from Phase 0; `/health` remains unauthenticated and unlimited. `app/auth/firebase.py` refactored to use `error_payload` (removes duplicated inline dict). `scripts/verify_docs.py` claims 12/13/14/18/19/20 upgraded from SKIP stubs to real PASS checks: #12 asserts all six endpoint decorators, #13 asserts `verify_fan_token` + `FanUid` usage, #14 asserts `STAFF_TOKEN` env read + `hmac.compare_digest` + `verify_staff_token` in routes, #18 asserts `load_default_graph()` + `app.state.graph` in `main.py`, #19 asserts `venue_repo.read_state` inside `post_navigate`, #20 asserts `K_SERVICE` gate + Entry #23 shape in `errors.py`. Tests: `tests/unit/test_edge_id.py` (10 tests: round-trip, direction-agnostic canonicalisation, empty/separator/malformed rejection), `tests/unit/test_errors.py` (6 tests: `K_SERVICE` present strips `detail` / absent keeps `detail`, `is_error_payload` positive+negative, `raise_error` wrapping), `tests/unit/test_staff_auth.py` (8 tests: missing env, valid, wrong token, missing/malformed/empty header, `K_SERVICE` gate), `tests/integration/conftest.py` (in-memory `FakeFirestoreClient` mocking Firestore at the client boundary per Entry #21 Layer 4; `integration_client` overrides `verify_fan_token` + `app.state.firestore_client_factory`), `tests/integration/test_navigate_endpoint.py` (13 tests including `test_missing_auth_returns_flat_error_shape` as the explicit Phase 3 double-wrap regression), `tests/integration/test_staff_closures.py` (12 tests including edge-closure canonicalisation round-trip, closures visible to `find_route` via fresh `read_state`, closures visible to `/navigate` through the real dependency chain). Machine validation: `make lint` clean (24 files), `make test` **142 passed, 1 skipped** (skip is an intentional adjacent-nodes fallback in the closure validation test), coverage **96.62%** on 782 statements (floor 95%), `make verify-graph` OK (36 nodes / 54 edges), `make verify-docs` **20 PASS / 0 SKIP / 0 FAIL** — Layer 2 goal met. Deploy + live-URL curl pending. No DECISIONS.md amendments — Phase 4A implements Entries #8/#13/#16/#17/#18/#19/#23 as already specified.
- **2026-07-14 — Phase 4A hotfix (addendum to the entry above).** Independent intent validation caught one live-production bug and two quality gaps; all three fixed before Phase 4B starts. (1) **Starlette `HTTPException` bypass** — FastAPI's default `HTTPException` handler was registered only for `fastapi.exceptions.HTTPException`; framework-produced 404 (unknown route) and 405 (wrong method) errors are raised by `starlette.exceptions.HTTPException` and leaked through as `{"detail": "Not Found"}` instead of the flat Entry #23 shape. Registered `_http_exception_handler` for `StarletteHTTPException` too. Regression tests in new `tests/integration/test_error_contract.py`: GET `/does-not-exist` → flat 404; DELETE `/health` → flat 405. (2) **Permanently-skipped test fixed** — `test_edge_between_real_but_nonadjacent_nodes_rejected` was using `gate_a_plaza` + `gate_j_plaza` (which ARE adjacent in the real graph), triggering its own `pytest.skip` fallback on every run. Replaced with `gate_a_plaza` + `gate_c_plaza` (verified non-adjacent), removed the `pytest.skip` branch. (3) **Coverage gaps closed** — added: rate-limit trip test in `test_error_contract.py` (60 fan requests OK, 61st → 429 transient with flat body); `test_profile_gemini_service_error_transient` covering `post_profile`'s `GeminiError` catch (502 transient); `test_navigate_closed_edge_forces_detour_through_endpoint` exercising `_decode_closures`'s `parse_edge_id` loop through the real `POST /navigate` endpoint (not just `find_route` directly); `test_open_edge_removes_closure` in `test_staff_closures.py` covering the `closed_edges.discard` branch; and `tests/unit/test_routes_helpers.py::test_handle_navigation_parse_route_impossible_raises_permanent_400` unit-testing the `RouteImpossible` → HTTP 400 branch (unreachable through the endpoint against the fully-connected real graph). Added `# pragma: no cover` on `_default_firestore_client_factory` (untestable one-line wrapper around `firestore.Client()`; decision documented explicitly rather than left as an oversight). Machine validation: `make lint` clean (25 files), `make test` **150 passed, 0 skipped** (up from 142/1), coverage **98.84%** on 783 statements (up from 96.62%; back above Phase 3's 98.88% baseline), `make verify-graph` OK, `make verify-docs` **20 PASS / 0 SKIP / 0 FAIL** unchanged. This is a hotfix to Phase 4A, not a new phase — Phase 4B (SVG renderer, `static/fan.html`, `static/staff.html`) remains untouched.
- **2026-07-14 — Phase 4B (MACHINE-VALIDATED, awaiting intent validation + deploy).** Shipped the frontend half of Phase 4 — the deterministic SVG renderer wired into `/navigate`, the static fan chat page, the static staff closure panel, and a CSP that permits exactly the origins those pages actually need. Files: `app/rendering/svg_renderer.py` (Entry #12 deterministic renderer; `render_route(route, graph, closed_nodes, closed_edges) -> str` returns a `data:image/svg+xml;base64,...` URI for `RouteFound`; `_canon(a, b)` direction-agnostically canonicalises the closed-edge input so a staff-side `"b__a"` still dashes the a↔b edge; `_render_node` applies the closed > origin > destination > intermediate > base precedence per DESIGN.md; compact `f"{v:g}"` numeric formatter to keep the base64 payload small; internally split into `_viewbox` / `_stadium_outline` / `_closed_edges_layer` / `_route_layer` / `_render_node` / `_summary_title` / `_build_svg` / `_to_data_uri` — every function under the 80-line cap, complexity ≤ 10, no reference to `gemini_factory`, `google.genai`, `GeminiClient`, or `explain_route` — verified by claim #22). `app/routes.py` `_handle_navigation_parse` now calls `render_route(route, graph, closed_nodes, closed_edges)` when the pathfinding result is `RouteFound`, and leaves `route_image=None` for `AmbiguousRequest` / `RouteBlocked` (Case 3 SVG for blocked routes is deferred per the Phase 4B spec). `static/style.css` (dark-mode palette locked in DESIGN.md — `#0F172A` bg / `#1E293B` surface / `#F1F5F9` text / `#94A3B8` muted / `#22D3EE` accent / `#F87171` warn — plus space tokens `--space-xs/sm/md/lg/xl` and app-shell layout classes; added `.visually-hidden` utility so `<label>`s can be screen-reader-only without violating "every input has a label"). `static/firebase-config.js` (public web-tier `apiKey`/`authDomain`/`projectId` — Firebase apiKey is a client identifier not a secret; placeholder committed with instructions for the operator to fill it in before serving). `static/fan.html` (single-page fan chat: titlebar with `role="status" aria-live="polite"` status text, messages `<section aria-live="polite">` for chat bubbles + inline route SVG `<img>`, composer form with real `<button>`. Loads Firebase compat SDK from `https://www.gstatic.com/firebasejs/10.14.1/` — allowed by CSP `script-src`. All script/style refs external — zero inline blocks). `static/fan.js` (~230-line IIFE: initializes Firebase, calls `signInAnonymously`, uses `auth.currentUser.getIdToken(false)` for every server call so tokens auto-refresh; boot flow issues `GET /profile` → 200 dispatches to chat mode, 404 dispatches to onboarding mode; `handleOnboardSend` calls `POST /profile` and dispatches on `profile_complete` / `profile_incomplete` / `profile_failed`; `handleNavSend` calls `POST /navigate` with 3-turn rolling history — Entry #10, #19 — and renders the returned directions + `route_image` inline via `img.src` + `img.alt`; every user-supplied string set via `textContent` never `innerHTML`; DESIGN.md microcopy locked in — placeholder "Where are you, and where do you want to go?", loading "Working out the route…", transient error "Something went wrong. Try again."). `static/staff.html` (token gate section with password `<input>` behind a `.visually-hidden` label; hidden by JS once auth succeeds; staff body has two panels: active closures list `<ul aria-live="polite">` and the add-closure form; all inputs labeled, external-scripts-only). `static/staff.js` (~200-line IIFE: token stored under `fanpath.staff_token` in `sessionStorage` — never `localStorage`, so token dies when tab closes, matches SECURITY.md posture on shared-secret staff auth; `apiFetch(method, body)` sends `Authorization: Bearer <token>` to `/staff/closures`; `handleTokenSubmit` stores the token then calls `loadClosures` — on 401 clears the token and re-shows the gate with error "Token rejected. Try again."; `renderClosures` sets status "N closure(s) active." or "No closures active.", appends `<li>` rows with Reopen `<button>`s that POST `{action:"open"}`; `handleAdd` POSTs `{action:"close"}`; every DOM write via `textContent`). `app/main.py` (`_CSP` string locked: `default-src 'self'; script-src 'self' https://www.gstatic.com; connect-src 'self' https://identitytoolkit.googleapis.com https://securetoken.googleapis.com; img-src 'self' data:; style-src 'self'; base-uri 'self'; frame-ancestors 'none'` — no `'unsafe-inline'`, no `'unsafe-eval'`, `img-src data:` present specifically to allow the inline base64 SVG from `/navigate`; `Content-Security-Policy` added to `_SECURITY_HEADERS`; `StaticFiles(directory=_STATIC_DIR)` mounted at `/static`). `scripts/verify_docs.py` extended with two new claims: **#21** verifies all five static files exist (`fan.html`, `fan.js`, `staff.html`, `staff.js`, `style.css`), asserts nothing under `app/` references `TemplateResponse` / `Jinja2Templates` / `from jinja2`, and confirms `StaticFiles(directory=` is present in `app/main.py`; **#22** verifies `app/rendering/svg_renderer.py` exists, defines `render_route`, contains no reference to `gemini_factory` / `google.genai` / `GeminiClient` / `explain_route`, and that `app/routes.py` calls `render_route`. Tests: `tests/unit/test_svg_renderer.py` (renderer unit tests using `tests/fixtures/small_graph.json`; asserts data-URI prefix, base64 body decodes to valid SVG XML, node precedence, closed-edge canonicalisation from both `"a__b"` and `"b__a"` inputs); flipped `assert body["route_image"] is None` at `tests/integration/test_navigate_endpoint.py:162` and `tests/integration/test_staff_closures.py:~310` to `assert isinstance(body["route_image"], str)` + `assert body["route_image"].startswith("data:image/svg+xml;base64,")` where the pathfinding result is `RouteFound`; kept `None` assertions for `AmbiguousRequest` and `RouteBlocked` responses. Machine validation: `make lint` clean (26 files), `make test` **164 passed** (up from 150), coverage **98.89%** on 892 statements (up from 98.84%; `app/rendering/svg_renderer.py` at 100%, `app/routes.py` at 99%), `make verify-graph` OK (36 nodes / 54 edges), `make verify-docs` **22 PASS / 0 SKIP / 0 FAIL** (up from 20) — Layer 2 goal met with the two new claims. No DECISIONS.md amendments — Phase 4B implements Entry #12 (deterministic renderer) and Entry #20 (static frontend) as already specified. Live-URL browser verification of the fan/staff pages pending deploy.
  - **2026-07-14 Phase 4B closeout addendum.** Two manual patches applied before deploy: (a) `Dockerfile` gained the missing `COPY static ./static` after `WORKDIR /app` — without it the base image had `app/` + `data/` but not `static/`, so the `/static/*` mount would 404 in production despite passing locally; (b) `static/firebase-config.js` `apiKey` replaced with the real Firebase Web API key from the `promptwars-c4-metlife` project (Firebase apiKey is a client identifier not a secret — per SECURITY.md §2). Accessibility bug fix: `static/fan.js` `img.alt` was a hardcoded generic label, but an SVG's internal `<title>` is not exposed to screen readers when the SVG is delivered via `<img src="data:...base64,...">` (the browser treats the img as an opaque bitmap). Added `buildAltText(directions)` helper that derives the alt from the actual Guide-Agent directions string, collapses whitespace, and caps total length at 140 chars with `"Route map: "` prefix + ellipsis truncation; falls back to the original generic label only on empty/whitespace/non-string input. Regression test `tests/unit/test_fan_js_alt_text.py` locks the contract with two always-run static-source assertions (the `img.alt = buildAltText(` call exists, the old hardcoded assignment is gone) plus a `node`-based behavioral test that extracts the helper via brace-counting and executes it against short/long/whitespace/empty inputs (skips gracefully when `node` is not on PATH — CI-portable, GH Actions ubuntu-latest ships node preinstalled). Machine validation refresh: `make lint` clean (26 files), `make test` **170 passed** (up from 164; +3 static assertions + 4 parametrised runtime), coverage **98.89%** unchanged, `make verify-graph` OK, `make verify-docs` **22 PASS / 0 SKIP / 0 FAIL**. Deploy: `gcloud run deploy fanpath-metlife --source . --region asia-south1 --quiet` at 2026-07-14T14:03:47Z → revision `fanpath-metlife-00007-62k`. Live curl verification: `/health` 200; all five `/static/*` assets 200; deployed `firebase-config.js` body confirmed to carry the real apiKey (no `REPLACE_WITH_*` placeholder — regression check that the local edit actually made it into the image); response headers include the full 7-directive CSP from `app/main.py`; end-to-end round trip — anonymous Firebase sign-in → `POST /profile` (three-field FanProfile persisted) → `POST /navigate "How do I get to Gate C from Section 128?"` returned `directions` (Guide-Agent prose) + `route_image` starting with `data:image/svg+xml;base64,` at 5,294 chars. Two Gemini flakes during the round-trip attempts produced `transient` 502s via the Entry #23 error contract (`gemini_error: ... response was not valid JSON` in Cloud Run logs, cleanly surfaced as `{"type":"error","category":"transient","message":"Upstream service error."}` to the client — no crash, no leaked stack trace). Phase 4 stays `MACHINE-VALIDATED` — the four items that require a real browser (fan.html loads and signs in without console error / navigate renders both text and a visible map / accessibility-flagged profile actually routes around `stairs_only` edges / staff.html toggle reflected on subsequent fan navigate) are called out in the closeout report for the manual-browser-check pass; the machine-checkable half of Phase 4B is done.
  - **2026-07-14 Phase 4 gap-closing pass (MACHINE-VALIDATED, awaiting intent validation + deploy).** Closed two gaps against the locked spec discovered after Phase 4B's closeout — the amenity-type destination half of Entry #9's Intent Agent contract, and the missing `GET /` root landing. Both had been anticipated but never wired end-to-end. This is a gap-closing pass on Phase 4, not a phase renumber. **(1) Amenity-type destination resolution** (new DECISIONS.md Entry #28 — gap-fill, not a supersession): `app/agents/schemas.py` — `ResolvedRequest.destination_amenity_type: AmenityType | None`, `destination: str | None`, `@model_validator(mode="after")` enforces exactly one of the two must be set (ValidationError caught by intent.py's outer try/except and surfaced as `GeminiServiceError("... failed validation")`); `app/agents/intent.py` — navigation prompt extended with all six amenity enum values and the mutually-exclusive JSON shape, `_dispatch_navigation` grounds only whichever zone field was actually set against `graph.nodes`; `app/pathfinding/engine.py` — `find_nearest_amenity(graph, origin, amenity_type, accessibility_flags, closed_nodes, closed_edges) -> RouteFound | RouteBlocked | RouteImpossible` implemented as a linear scan over the 36-node graph (`_candidate_zones_for_amenity` filters, `_select_best_amenity_result` reuses single-pair `find_route` per candidate then picks the lowest `total_walk_time_minutes`; when no candidate is reachable but some were closure-blocked, returns a `RouteBlocked` naming the amenity, otherwise `RouteImpossible`); `app/routes.py` — extracted `_resolve_route` helper that branches on `destination_amenity_type` (calls `find_nearest_amenity`) vs `destination` (calls `find_route`), keeping `post_navigate` under the 80-line cap; `app/agents/guide.py` — `_AMENITY_NOTE_TEMPLATE` appended to the prompt when `explain_route(route, ..., amenity_type=...)` is called with a resolved amenity destination, so the Guide names the resolved zone and mentions it's a "KIND of place" (Entry #25 tone-preserving). **(2) Root landing**: `app/main.py` — `GET /` now returns `RedirectResponse(url="/static/fan.html")`, unauthenticated and unlimited (bare landing URL now lands the browser on the fan chat instead of a 404). **(3) verify-docs**: `scripts/verify_docs.py` gained real `claim_23_amenity_resolution` checking `destination_amenity_type` + `@model_validator` in schemas.py, `def find_nearest_amenity` in engine.py, and `find_nearest_amenity` call in routes.py. **Tests**: +8 in `tests/unit/test_pathfinding.py` (`_with_amenities` helper cloning frozen Graph/Node dataclasses; basic, lowest-walk-time selection, accessibility-flag changes choice, closures affect choice, no matching zone, unknown origin, all candidates blocked, only-via-island); +3 in `tests/contract/test_intent_agent.py` (amenity-only resolved, rejects both destination fields set, rejects neither destination field set — both rejections surface as `GeminiServiceError("... failed validation")` via the outer try/except); +1 in `tests/integration/test_navigate_endpoint.py` (`test_navigate_amenity_type_destination_resolves_zone` — Intent returns `destination_amenity_type: "restroom"`, endpoint returns SVG, Guide prompt contains "restroom" and "KIND of place"); +2 in `tests/test_main.py` (`test_root_redirects_to_fan_html`, `test_root_redirect_unauthenticated`). Machine validation: `make lint` clean (all files under 80-line cap, max complexity 10), `make test` **184 passed, 0 skipped** (up from 170; +14 covering the two gaps), coverage **98.94%** on 917 statements (up from 98.89%; floor 95%), `make verify-graph` OK (36 nodes / 54 edges), `make verify-docs` **23 PASS / 0 SKIP / 0 FAIL** (up from 22 with the new claim #23). DECISIONS.md amended: Entry #28 added — amenity-type destination resolution — closing the "or amenity type" half of Entry #9's Intent Agent contract that had been in the DECISIONS.md text since day one but had no paired implementation until this pass. No supersession. Live-URL deploy + curl round-trip verification of `GET /` redirect and amenity-type navigation pending.
- **2026-07-14 — Phase 5 (DONE).** Presentation pass — no new features, no architecture changes. **(1) Code Quality:** added docstrings to all public functions and Pydantic models across `app/routes.py` (5 endpoint handlers), `app/agents/schemas.py` (7 discriminated-union models), `app/schemas.py` (6 HTTP-boundary models), `app/main.py` (`create_app()`), `app/firestore/venue_state.py` (`VenueState`), `app/rate_limit.py` (`rate_limit_key()`); cleaned stale module docstrings in `venue_state.py` ("not wired yet" — it is) and `guide.py` ("SVG rendering does not exist until Phase 4" — it does); function-length 0 violations / ruff complexity 0 violations / 0 dead code or debug prints. **(2) PS Alignment:** all 8 grilling-session hard cases traced to tests and code paths — 7 fully covered, 1 (Lot K parking) correctly documented as out-of-scope in both README.md and DECISIONS.md. **(3) Security:** added single server-side retry on Gemini JSON-parse failure in `app/agents/intent.py` (`extract_profile` and `parse_navigation_request`) — one retry, no backoff, logged at WARNING level; fixed pip-audit documentation drift by actually adding `pip-audit>=2.7` to dev dependencies, `make audit` target, and CI pipeline step; completed all 10 OWASP Top 10 entries in SECURITY.md with verified-against-code content (was 4 written / 6 stubs; now 10/10). **(4) Testing:** 186 tests, 98.95% coverage, 0 `pytest.skip` calls (one conditional `skipif` on `node` binary availability in `test_fan_js_alt_text.py` — acceptable). **(5) Accessibility:** four manual-browser checks from Phase 4 closeout deferred — cannot be performed from this environment (no browser); flagged for Phase 6 or manual verification before submission. Machine validation: `make lint` clean, `make test` **186 passed**, coverage **98.95%**, `make verify-graph` OK (36/54), `make verify-docs` **23 PASS / 0 SKIP / 0 FAIL**. Deployed: revision `fanpath-metlife-00009-tnh`, live URL `/health` 200 at 2026-07-14T15:36:05Z. No DECISIONS.md amendments — no architectural changes this phase.
- **2026-07-14 — Phase 6 (DONE).** Final gauntlet and documentation freeze — no new features, no code changes, no deploy. **(1) Fresh-venv machine gauntlet:** created `.venv-final` (Python 3.13.7), installed `.[dev]` cleanly. `make lint` clean (26 files, max 80 lines, 0 violations). `make test` **186 passed**, 1 warning, coverage **98.95%** on 947 statements (floor 95%). `make verify-graph` OK (36 nodes, 54 edges, 0 warnings). `make verify-docs` **23 PASS / 0 SKIP / 0 FAIL**. `pip-audit` clean (pip itself was 25.2 with 6 CVEs; upgraded to 26.1.2; no application-dependency vulnerabilities). **(2) Live-URL gauntlet** against `https://fanpath-metlife-973486326780.asia-south1.run.app` (revision `fanpath-metlife-00009-tnh`): `GET /health` → 200 `{"status":"ok"}`; `GET /` → 307 to `/static/fan.html`; all six `/static/*` assets 200; `firebase-config.js` body has real apiKey, zero `REPLACE_WITH_*` placeholders; `/health` response includes all 7 CSP directives; unauthenticated `GET /profile` → flat 401 Entry #23 shape; unauthenticated `GET /staff/closures` → flat 401; unknown route `/nope-final-check` → flat 404 (not `{"detail":"Not Found"}`). Fan round trip: Firebase anonymous sign-in → `POST /profile` → 200 with `seat_section`, `accessibility_flags`, `preferred_language`, `created_at`; `POST /navigate "How do I get to Gate C?"` → 200 with `directions` + 5,294-char `data:image/svg+xml;base64,...` route image. Amenity round trip: `POST /navigate "Where is the nearest restroom?"` → 200 with directions naming resolved zone ("concourse 100 south") + 4,786-char SVG data URI. Staff round trip: `GET /staff/closures` → empty state; `POST /staff/closures` close `gate_b_plaza` → reflected in GET; fan `POST /navigate` to Gate B with closure active → `RouteBlocked` text-only response ("Gate B Plaza area is currently closed"); `POST /staff/closures` reopen → state cleared. One Gemini transient flake (502) during profile onboarding recovered on client retry — known pattern, documented. **(3) Documentation freeze:** cross-checked all factual claims in DECISIONS.md (28 entries), PROGRESS.md, README.md, SECURITY.md, and CLAUDE.md against measured values. All numbers match (186 tests, 98.95% cov, 23/0/0 verify-docs, 36 nodes / 54 edges, revision `fanpath-metlife-00009-tnh`). One stale text removed: SECURITY.md's OWASP section still had the "To be filled in during Phase 5" stub header despite being fully complete — removed. PROGRESS.md Phase 6 row flipped to DONE. README.md status updated to Phase 6 of 6 complete. **(4) Four manual-browser checks remain outstanding** (same as Phase 4B/5 — cannot be performed from CLI): (a) `fan.html` loads and completes Firebase Anonymous sign-in with no console error; (b) navigate renders both text and visible map image in chat UI; (c) accessibility-flagged profile routes around `stairs_only` edges (visually distinguishable); (d) `staff.html` toggle reflected in subsequent fan navigate. Explicitly NOT marked as verified — flagged for human confirmation before submission. No DECISIONS.md amendments. No code changes. No deploy.

---

## Deviation tracker

Any place the build diverged from the 28 locked decisions or the phase plan. Each entry: what changed, why, and whether DECISIONS.md was updated with a supersession.

- **2026-07-14 — Tool allocation:** Phases 2 and 3 were both built on Copilot, not Claude Code as the Jul-13 revised schedule table specified. Claude Code was evaluated for Phases 2-3 (discriminated-union contract-fidelity risk) but not adopted after Copilot's clean Phase 2 track record. Not a DECISIONS.md architectural entry — no supersession needed. Correction note added to the "Revised schedule" section above so the table isn't misleading.
- **2026-07-14 — Un-spec'd file:** Copilot created `CLAUDE.md` (150 lines, Claude Code configuration) during Phase 3 without it being in the prompt spec. File is harmless and correctly summarizes DECISIONS.md governance. Kept, but noted here since it was not in the Phase 3 exit criteria.
- **2026-07-14 — Amenity-type destination gap (closed):** Entry #9 had specified since day one that the Intent Agent's `ResolvedRequest` could name "a specific zone **or an amenity type**", but Phase 3 shipped only the specific-zone half — `ResolvedRequest.destination: str` with no amenity-type field, no `find_nearest_amenity` in the pathfinding engine, no wiring in `/navigate`. Not caught in Phase 3 intent validation because the amenity-type contract was buried in Entry #9's prose rather than surfaced as a distinct schema field, and no test exercised the missing branch. Phase 4B closeout finally surfaced it. Closed in the 2026-07-14 gap-closing pass (see running-log entry above). DECISIONS.md **was** updated — new **Entry #28** added as a gap-fill, not a supersession (Entry #9's original text already anticipated amenity-type destinations; Entry #28 makes the schema + pathfinding contract explicit and links back to #9). Also closed the missing `GET /` root landing (bare live URL returned 404 instead of redirecting to `/static/fan.html`) — deliberate zero-content root omission, not architectural, no DECISIONS.md entry needed.
- **2026-07-14 — Conversation history role mismatch (found during Phase 6 manual testing).** `fan.js` pushed `role: "user"` / `role: "assistant"` but `ConversationTurn.role` only accepts `"fan"` / `"guide"`. Every second navigate call failed with a 422 masked by the error handler. Went undetected through Phases 3-5 because every integration test passed `history: []` — zero tests exercised non-empty history. The frontend was the only code path that populated history, and it did so with the wrong values. Not a DECISIONS.md entry — no architecture change, just a frontend-backend contract mismatch. Gap closed with 3 regression tests (1 integration, 2 static-source assertions).

---

## Live URL health log

Every deploy is logged with a timestamp and a `curl` result. This is the Efficiency probe defense: never a mystery about when the live URL last returned 200.

| Timestamp (UTC) | Commit SHA | `curl /health` result | Deploy source (phase) |
| --------------- | ---------- | --------------------- | --------------------- |
| 2026-07-13T16:30:35Z | c81776d | `{"status":"ok"}` — HTTP 200 | Phase 0 (skeleton) |
| 2026-07-14T~03:57Z (approx — exact deploy timestamp not captured; inferred from first successful `/profile` write) | 8ff4cc2 | `{"status":"ok"}` — HTTP 200; full flow verified (`/profile` onboarding + `/navigate` returning real Gemini-backed directions) | Phase 4A (six-endpoint API surface) |
| 2026-07-14 (exact timestamp not captured — flag for next deploy: always run `date -u` alongside the health curl) | b22dd79 | Not explicitly re-curled in the report; inferred healthy from the two confirming curls that followed (unauth `/profile` → flat 401, `/nope` → flat 404, both well-formed JSON responses) | Phase 4A hotfix (Starlette exception handler + coverage) |
| 2026-07-14T14:03:47Z (deploy) / 2026-07-14T14:09:17Z (re-curl) | 00854be | `/health` 200; five `/static/*` assets 200; live `firebase-config.js` confirmed to carry the real apiKey (no `REPLACE_WITH_*` placeholder — regression check that the closeout's local edit made it into the image); full 7-directive CSP header present; anonymous sign-in → `POST /profile` (three-field FanProfile persisted) → `POST /navigate` returned Guide-Agent directions + `route_image` starting with `data:image/svg+xml;base64,` at 5,294 chars. Two Gemini JSON-parse flakes surfaced cleanly as `{"type":"error","category":"transient"}` via the Entry #23 contract — no crash, no leaked stack trace, retried and succeeded. | Phase 4B + closeout (SVG renderer, static frontend, CSP, Dockerfile static-copy fix, real firebase apiKey, alt-text a11y fix); revision `fanpath-metlife-00007-62k` |
| 2026-07-14T14:57:15Z | ced3edf | `/health` 200; `GET /` → 307 `Location: /static/fan.html`; `/static/fan.html` 200; full CSP header present; amenity round trip (anonymous sign-in → `POST /profile` → `POST /navigate "where is the nearest restroom from gate A?"`) returned directions naming resolved zone "100 North Concourse" + 5,114-char `data:image/svg+xml;base64,` route image (one profile-side Gemini flake surfaced as `{type:"error",category:"transient"}` on the first pass — recovered on retry) | Phase 4 gap-closing pass (Entry #28 amenity resolution + `GET /` redirect); revision `fanpath-metlife-00008-d7k` |
| 2026-07-14T15:36:05Z | a203c2a | `/health` 200 | Phase 5 (presentation pass — docstrings, Gemini retry, OWASP Top 10, pip-audit in CI); revision `fanpath-metlife-00009-tnh` |
| 2026-07-14T16:09:19Z | 0b687e0 | `/health` 200; full live-URL gauntlet green (all static assets 200, CSP 7 directives, flat error shapes, fan round trip with SVG, amenity round trip naming resolved zone, staff closure round trip) | Phase 6 (final gauntlet — no deploy, same revision `fanpath-metlife-00009-tnh`) |
| 2026-07-14T16:30:26Z | a792bbb | `/health` 200; two-consecutive-navigate round trip verified (1st message empty history → 200; 2nd message with `role:"fan"/"guide"` history → 200 — confirms the role-mismatch bug is fixed) | Phase 6 hotfix (conversation history role fix); revision `fanpath-metlife-00010-wcc` |

---

## Rubric self-assessment (Phase 6)

Scored criteria mapped to what actually shipped and any known gap.

### Code Quality (High weight)

**Shipped:** ruff with C901/PLR0912/PLR0915, max-complexity 10. AST-based function-length cap at 80 lines — 0 violations across 26 files. Docstrings on all public functions and Pydantic models. No dead code, no debug prints, no TODO stubs. Discriminated unions for all agent outputs and pathfinding results (never bare strings). Deterministic pathfinding between agents — model never invents a route. Coverage 98.95% on 186 tests (floor 95%). `make verify-docs` 23 PASS / 0 SKIP / 0 FAIL.

**Known gap:** None identified.

### Problem Statement Alignment (High weight)

**Shipped:** Natural-language indoor navigation for a real venue (MetLife Stadium, FIFA World Cup 2026 final). Ambiguity resolution via Gemini Intent Agent (vague landmarks, embedded accessibility constraints, amenity-type destinations). Five-language responses (en/es/fr/pt/ar). Accessibility-aware routing (wheelchair, no_stairs, stroller, visual_impairment). Dynamic closures with zero staleness (staff toggle → immediate effect on fan routing). Schematic route SVG. Six amenity types. Multi-turn context (3-turn rolling window). All 8 grilling-session hard cases traced to tests — 7 fully covered, 1 (Lot K parking) documented as out-of-scope.

**Known gap:** Parking-lot navigation explicitly out of scope (Entry #4). No RouteBlocked SVG (deferred, Entry #22 Case 3). No multi-marker amenity SVG (deferred, Entry #22 Case 4).

### Security (Medium weight)

**Shipped:** Full OWASP Top 10 walkthrough in SECURITY.md (10/10 entries, verified against code). Firebase Anonymous Auth on fan endpoints (server-side token verification). Constant-time staff token comparison (`hmac.compare_digest`). `K_SERVICE`-gated error detail (no stack traces in production). CSP with 7 directives (no `unsafe-inline`, no `unsafe-eval`). `pip-audit` in CI. Rate limiting (60/min fan, 30/min staff). All DOM writes via `textContent` (no `innerHTML`). Pydantic `extra="forbid"` on all request models. Edge/node target validation against real graph before Firestore mutation.

**Known gap:** Prompt injection partially mitigated (deterministic Dijkstra limits blast radius, but Intent Agent can still be confused). Shared-secret staff auth (documented, deliberate). Anonymous auth device-bound (documented, deliberate). All three are stated limitations in SECURITY.md.

### Efficiency (Medium weight)

**Shipped:** Live URL returns 200 consistently (7 health-log entries across Phases 0-6). Full fan round trip works (sign-in → profile → navigate with SVG). Amenity round trip works (resolves nearest zone). Staff closure round trip works (close → navigate affected → reopen). Known Gemini JSON-parse flake mitigated with single server-side retry. All transient errors surface cleanly via Entry #23 contract — no crashes, no leaked traces.

**Known gap:** Gemini JSON-parse flakes still occur (~1 in 3 requests during rapid testing), but always surface as `{"type":"error","category":"transient"}` and succeed on client retry. Not a crash.

### Testing (Low weight)

**Shipped:** 186 tests, 98.95% coverage. Four testing layers per Entry #21: graph validation script, pathfinding unit tests on synthetic graphs, agent contract tests with mocked Gemini, integration tests with full FastAPI test client and mocked Firestore. 0 `pytest.skip` calls. 1 conditional `skipif` on `node` binary (alt-text behavioral test — graceful skip when node not available).

**Known gap:** None. Coverage exceeds the 95% floor by ~4 percentage points.

### Accessibility (Low weight)

**Shipped (machine-verified):** `aria-live="polite"` on chat message container and staff closure list. All `<img>` tags have `alt` attributes (derived from Guide Agent directions, not hardcoded). All `<input>` and `<button>` elements have associated `<label>` or `aria-label`. Semantic HTML (`<main>`, `<section>`, `<button>`). `.visually-hidden` utility for screen-reader-only labels. Focus-visible rings with `--color-accent`. SVG embeds `<title>` element. Color never sole carrier of meaning (closures use both `--color-warn` AND text/dashing).

**Known gap (human-browser-only):** Four manual checks remain unverified — (1) fan.html loads with no console error, (2) route image renders visibly, (3) accessibility-flagged profile produces visually distinct route, (4) staff toggle reflected in fan UI. These cannot be verified from CLI.

---

## Phase 6 hotfix log

- **2026-07-14 — Conversation history role mismatch (real bug, found during manual live-URL testing).** `static/fan.js` `pushHistory()` pushed `{role: "user"}` / `{role: "assistant"}` but `ConversationTurn.role` in `app/agents/schemas.py` only accepts `Literal["fan", "guide"]`. Effect: every fan's first navigate call works (empty history), every second call fails with a 422 validation error that `fan.js`'s `errorMessage()` doesn't recognise (it only checks `body.type === "error"`, not FastAPI's default `{"detail": [...]}` shape), so the real cause is silently masked behind the generic fallback message. **Root cause of the test gap:** all 186 prior tests passed `history: []` — zero tests exercised non-empty history. The schema mismatch was invisible at the backend level because the frontend was the only caller that ever populated history, and it did so incorrectly. **Fixes:** (1) `static/fan.js:181-182`: `role: "user"` → `role: "fan"`, `role: "assistant"` → `role: "guide"`. (2) `static/fan.js:errorMessage()`: added fallback branch that logs `console.error(body)` when `body.detail` is an array (FastAPI validation error shape), so future request-shape mismatches surface in dev tools instead of silently falling through. (3) `tests/integration/test_navigate_endpoint.py`: added `test_navigate_with_nonempty_history_succeeds` — sends `POST /navigate` with a non-empty, correctly-shaped history and asserts 200. (4) `tests/unit/test_fan_js_alt_text.py`: added `test_push_history_uses_fan_guide_roles` and `test_push_history_does_not_use_user_assistant_roles` — static-source assertions that grep `pushHistory`'s body for the correct role strings, same pattern as the existing alt-text static checks. Machine validation: `make lint` clean, `make test` **189 passed** (up from 186), coverage **98.95%** unchanged, `make verify-docs` **23 PASS / 0 SKIP / 0 FAIL**. Test count in README.md and PROGRESS.md Phase 6 entry updated to 189.